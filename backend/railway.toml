[build]
builder = "NIXPACKS"
buildCommand = "cd backend && npm run build"

[deploy]
startCommand = "node /app/backend/dist/main"
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3